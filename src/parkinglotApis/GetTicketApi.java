package parkinglotApis;

import data.ParkingSpot;
import data.Ticket;
import data.Vehicle;

public class GetTicketApi {

    // get Ticket
    // form -vehicle info enter -store -ticketnumber generate and store in db and id will be unique and that id can be treated as ticket number
    // or second thing we can do give responsibility to
    public Ticket getTicket(Vehicle vehicle, ParkingSpot parkingSpot)
    {
        return null;
    }
}
